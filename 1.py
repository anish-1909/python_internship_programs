# Input list
numbers = [1, 2, 3, 4, 5]

# Using lambda to double each element
doubled = list(map(lambda x: x * 2, numbers))

# Output
print("Original list is", numbers)
print("Doubled list is", doubled)